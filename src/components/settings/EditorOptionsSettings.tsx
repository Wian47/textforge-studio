import React from 'react';

interface EditorOptionsSettingsProps {
  isOpen: boolean;
  onClose: () => void;
}

export default function EditorOptionsSettings({ isOpen, onClose }: EditorOptionsSettingsProps) {
  // Component removed to fix UI responsiveness issues
  return null;
}
